watch:
    directories:
        - app
        - resources
        - routes
        - tests
    fileMask: '*.php'
notifications:
    passingTests: true
    failingTests: true
phpunit:
    binaryPath: vendor/bin/phpunit
